<h1>Articles Search</h1>

<ul>
  <% @articles.each do |article| %>
    <li>
    <h3>
      <a href="<%= post_url article %>" title="<%= raw article.title %>"><%= raw article.title %></a>
    </h3>
    </li>
  <% end %>
</ul>
