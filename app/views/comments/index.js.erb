$('#comments-list').append("<%= j render 'comments/list',  :comments => @comments, :comments_normal_count => @comments_normal_count, :commentable_type => @commentable_type, :commentable_id => @commentable_id %>");
