- comment ||= @comment
.comment.cf id="comment-#{comment.id}"
  .avatar.left
    a[href="javascript:void(0)"]
      img[src="#{comment.user.try(:avatar)}" alt=""]
  .comment-wrapper
    .postmeta
      a.user_info_name[href="#"] = comment.user.try(:name)
      = " • #{comment.human_created_at}"
    .commemt-main
      p  = comment.content
    .opts
